server:
  port: 9999
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/houliliu/springcloud
          username:
          password:
          search-paths:
      label: master